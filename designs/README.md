# designs/

One folder per project/screen area (e.g. `dashboard/`, `onboarding/`,
`settings/`), not per person — see the root `CLAUDE.md` for why, and for
the full contribution workflow.

Create a new folder here when starting work on a new area — just add
your first file at the path, no separate step needed.
